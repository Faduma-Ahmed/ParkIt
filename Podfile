# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Park-It' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Park-It

  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'Stripe'
  pod 'Alamofire'
  pod "MonthYearPicker"

  target 'Park-ItTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Park-ItUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
